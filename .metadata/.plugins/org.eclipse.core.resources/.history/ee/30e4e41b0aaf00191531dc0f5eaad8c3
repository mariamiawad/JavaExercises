import java.util.*;

import javax.swing.text.TabableView;

import org.omg.CORBA.Current;
import org.w3c.dom.ls.LSException;

import java.io.*;

//class Node {
//    Node left;
//    Node right;
//    int data;
//    
//    Node(int data) {
//        this.data = data;
//        left = null;
//        right = null;
//    }
//}

class TopView {

	/*
	 * 
	 * class Node int data; Node left; Node right;
	 */

	static List<Node> list = new LinkedList<Node>();
	static List<Integer> listInt = new LinkedList<Integer>();

	public static void topView(Node root) {
		Node tempNode = null;
		if (root == null) {
			return;
		}
//		if(list.isEmpty()) {
//			list.add(root);
////			if(root.right!=null) {
////			topView(root.right);
////			}
////			if(root.left != null) {
////			list.add(root.left);
////
////			topView(root.left);
////			}
//		
//		}
//		if(root.right !=null) {
//			 if(root.right.left != null  )
//			{
//			list.add(root.right.left);
//			topView(root.right.left);
//			}
//		 if(root.right.left == null  )
//			 for (Node node : list) {
//				 
//				 System.out.println(node.data + " ");
//				 list.remove(node);
//			}
//		}

		if ((root.right == null && root.left == null)) {

			list.add(root);
			listInt.add(root.data);
			for (int node : listInt) {
				System.out.println(node + " ");
			}
			for (Node node : list) {
				if (node != null) {
					tempNode = node;
					break;
				}

			}
//			topView(tempNode.right);
		}
//		if (root.left == null) {
//			list.add(root);
//			for (Node node : list) {
//				System.out.print(node.data + " ");
//			}
//		}

		if (root.left != null) {

			list.add(root);
			listInt.add(root.data);
			topView(root.left);

		}
//		if (root.right != null) {
//
//			listInt.add(root.data);
//			topView(root.right);
//
//		}
		if (root.right != null) {

			list.add(root);
			listInt.add(root.data);
			topView(root.right);
		}
		if (tempNode != null) {
			root = tempNode;
			list.add(root.right);
			listInt.add(root.right.data);
			topView(root.right);

		}

//		if (current.left != null) {
//			
//			list.add(root);
//			topView(current.left);
//		}
	}

//		   if( root.right != null && root.left==null) {
//			 list.add(root.left);
//			 for (Node node : list) {
//				 System.out.print(node.data);
//			}

//		}

	public static Node insert(Node root, int data) {
		if (root == null) {
			return new Node(data);
		} else {
			Node cur;
			if (data <= root.data) {
				cur = insert(root.left, data);
				root.left = cur;
			} else {
				cur = insert(root.right, data);
				root.right = cur;
			}
			return root;
		}
	}

	public static void main(String[] args) {
		Scanner scan = new Scanner(System.in);
		int t = scan.nextInt();
		Node root = null;
		while (t-- > 0) {
			int data = scan.nextInt();
			root = insert(root, data);
		}

		scan.close();
		topView(root);
	}
}